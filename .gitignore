# dependencies
node_modules/

# build output
dist/

# photos live in the Cloudflare R2 bucket, never in Git.
# This folder is left over from the old local-resizing workflow; nothing
# reads it now, but it stays ignored in case you keep scans here.
photos/

# editor / OS junk
.DS_Store
*.local
.vscode/
.idea/
