{
  "name": "photo-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A self-hosted gallery for 35mm photography",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "dev": "npm run generate && vite",
    "build": "npm run generate && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.33.4",
    "vite": "^6.0.0"
  }
}
